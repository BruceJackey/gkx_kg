# 知识图谱构建平台 - 产品需求文档 (PRD)

**文档版本**: v1.0  
**最后更新**: 2026-04-21  
**产品负责人**: AI Team  
**目标发布日期**: 2026 Q2

---

## 1. 产品概述

### 1.1 产品简介
知识图谱构建平台是一个面向企业和研究机构的专业化知识图谱构建与管理系统。平台提供从数据管理、算法训练、流程编排到图谱服务的全生命周期解决方案，支持深度学习、大语言模型和规则算法等多种技术路线，帮助用户快速构建高质量的领域知识图谱。

### 1.2 产品定位
- **目标用户**: 知识图谱工程师、算法工程师、数据科学家、领域专家
- **应用场景**: 科技文献分析、医疗知识图谱、企业知识管理、智能问答系统
- **核心价值**: 提供低代码化的知识图谱构建能力，降低技术门槛，提升构建效率

### 1.3 产品目标
- 支持多种算法类型（深度学习、LLM、规则算法）的统一管理
- 提供完整的模型训练、部署、监控能力
- 支持灵活的流程编排，满足不同构建场景
- 提供丰富的图谱服务能力，支持可视化、查询、分析
- 构建开箱即用的知识图谱应用

---

## 2. 用户角色

### 2.1 算法工程师
- **职责**: 训练和优化知识图谱构建算法
- **核心需求**: 
  - 管理训练数据集
  - 配置训练参数
  - 查看训练过程和结果
  - 管理模型版本
  - 部署模型服务

### 2.2 知识图谱工程师
- **职责**: 设计和执行知识图谱构建流程
- **核心需求**:
  - 选择和组合算法
  - 配置构建流程
  - 监控流程执行
  - 管理图谱数据

### 2.3 领域专家
- **职责**: 使用知识图谱应用进行领域分析
- **核心需求**:
  - 图谱可视化浏览
  - 智能问答
  - 知识推理
  - 关系发现

---

## 3. 功能架构

```
知识图谱构建平台
├── 数据管理
│   └── 数据看板
├── 算法管理
│   ├── 算法列表
│   ├── 数据集管理
│   └── 任务管理
├── 流程管理
│   ├── 流程列表
│   └── 部署管理
├── 图谱服务
│   ├── 服务列表
│   └── API管理
└── 图谱应用
    └── 应用中心
```

---

## 4. 详细功能需求

## 4.1 数据管理模块

### 4.1.1 数据看板

**功能描述**: 提供数据源统计和管理功能

**核心功能**:
- **数据概览卡片**
  - 总数据量统计
  - 数据源数量统计
  - 本月新增数据统计
  - 数据质量评分
  - 可视化图表（饼图、柱状图）

- **数据源管理**
  - 数据源列表展示（名称、类型、规模、更新时间、状态）
  - 数据源类型：学术文献、专利数据、研究数据库等
  - 支持刷新、查看详情、配置、删除等操作

- **数据采样**
  - 预览数据样本
  - 查看数据字段结构
  - 数据质量检查

- **API文档**
  - 数据接入API说明
  - 请求/响应示例
  - 参数说明

**交互要求**:
- 数据源表格支持排序、筛选
- 状态用颜色标识（运行中-绿色、已停止-灰色）
- 操作按钮分组展示

---

## 4.2 算法管理模块

### 4.2.1 算法列表

**功能描述**: 分类管理知识图谱构建算法

**算法分类**:
1. **实体抽取算法** (3个算法)
   - LSTM-CRF
   - BERT-LSTM-CRF
   - LLM-Based Entity Extraction

2. **关系抽取算法** (3个算法)
   - CNN-based Relation Extraction
   - BERT-based Relation Extraction
   - LLM-based Relation Extraction

3. **实体消歧算法** (2个算法)
   - Entity Linking
   - Neural Disambiguation

4. **知识推理算法** (2个算法)
   - Rule-based Reasoning
   - Neural Reasoning

5. **图嵌入算法** (2个算法)
   - Node2Vec
   - GraphSAGE

**核心功能**:
- 三级导航结构：算法分类 → 分类详情 → 算法详情
- 算法类型标识：深度学习、大模型、规则算法
- 训练支持标识：支持训练 / 不支持训练
- 算法状态：已部署、训练中、未部署

**算法详情页**包含四个Tab:

#### Tab 1: 算法介绍
- 基本信息（ID、名称、类型、版本、负责人、创建/更新时间）
- 算法简介
- 适用场景
- 输入/输出格式说明
- 性能指标（F1、Precision、Recall、推理速度）
- 注意事项

#### Tab 2: 模型版本
- **功能描述**: 管理该算法的所有训练模型版本
- **模型信息展示**:
  - 版本号、状态（已部署/就绪）
  - 训练时间、模型大小
  - 性能指标（F1、Precision、Recall）
  - 部署次数、训练任务ID、数据集
  - **模型备注**（黄色高亮显示）
- **操作功能**:
  - 查看详情：展示完整的模型信息和训练参数
  - 部署：配置部署参数并部署模型
  - 下载：下载模型文件
- **版本说明**:
  - 每次训练成功自动生成新版本
  - 支持多版本并存
  - 建议定期清理旧版本

#### Tab 3: 训练记录
- 训练历史列表
- **数据格式要求说明**（蓝色信息框）:
  - 实体抽取：JSONL格式，包含text和entities字段，示例代码
  - 关系抽取：JSONL格式，包含head_entity、tail_entity、relation字段
  - 实体消歧：JSONL格式，包含mention、context、candidates、correct_entity字段
  - 数据准备提示和建议
- 训练任务详情（任务ID、版本、数据集、发起人、时间、状态、结果）

#### Tab 4: 部署管理
- 部署记录列表
- 部署详情（服务名称、地址、API入口、状态、时间）
- 支持重新部署

**训练功能**:
- **前置条件**: 算法必须支持训练（trainable: true）
- **训练配置**:
  1. **数据集选择**
     - 从数据集管理中选择已上传的数据集
     - 显示数据集详细信息（类型、格式、记录数、大小）
     - 支持跳转到数据集管理上传新数据集
  
  2. **模型备注** (新增)
     - 可选输入框，支持多行文本
     - 建议填写训练特点，如：使用的数据集、优化方向、实验性版本等
     - 示例：
       - "使用医疗数据集训练，针对疾病、药物、症状实体识别优化"
       - "基线版本，使用默认参数训练"
       - "科技文献专用版本，针对方法名、领域术语识别优化"
  
  3. **训练参数配置**
     - 训练轮数 (Epochs)
     - 批次大小 (Batch Size)
     - 学习率 (Learning Rate)
     - 优化器 (Adam/SGD/AdamW)
     - 验证集比例 (10%/20%/30%)
     - 早停轮数 (Early Stopping)
     - 模型参数（最大长度、隐藏层大小、Dropout等）
  
  4. **计算资源配置**
     - GPU类型（V100/A100/T4）
     - GPU数量

- **训练建议**（蓝色提示框）:
  - 建议使用至少5000条标注样本
  - 学习率建议从0.001开始调整
  - 开启早停防止过拟合
  - **建议添加备注说明训练特点**
  - 训练成功后模型自动保存到"模型版本"tab

**部署功能**:
- **模型选择**: 
  - 从"模型版本"tab中选择要部署的模型
  - 显示模型详细信息（版本、F1、训练日期、大小、备注）
- **服务配置**:
  - 服务名称
  - 计算资源（CPU/GPU）
  - 副本数量
  - 最大并发数
  - 请求超时时间
  - 自动扩缩容配置
- **部署说明**（绿色提示框）:
  - 只能部署已训练完成的模型
  - 建议至少2个副本保证高可用
  - GPU适合高并发，CPU适合低频调用

---

### 4.2.2 数据集管理

**功能描述**: 分类管理训练数据集

**数据集分类**:
1. 实体抽取数据集
2. 关系抽取数据集
3. 实体消歧数据集
4. 知识推理数据集
5. 图嵌入数据集

**核心功能**:
- 三级导航：数据集分类 → 分类详情 → 数据集列表
- **数据集信息展示**:
  - 名称、大小、记录数、格式（JSONL/CSV/TXT）
  - 上传日期、状态（就绪/处理中/错误）
  - 描述信息
  - 使用该数据集的算法列表
  - **数据示例**（Markdown格式，支持代码块）

- **数据示例功能** (核心特性):
  - 每个数据集可以包含Markdown格式的数据示例
  - 示例包含：
    - 代码块展示实际数据格式
    - 字段说明
    - 格式要求
  - 点击"查看示例"按钮弹窗展示
  - 支持代码高亮和格式化展示

- **上传数据集**:
  - 数据集名称
  - 数据集类型（自动匹配分类）
  - 描述信息
  - **数据示例** (支持Markdown格式)
    - 可选输入框，支持代码块、加粗、列表等格式
    - 帮助其他用户理解数据格式
    - 示例模板提示
  - 文件选择（支持JSONL、CSV、TXT，最大500MB）

- **操作功能**:
  - 查看示例
  - 下载
  - 删除

**数据格式说明**（蓝色信息框）:
- 实体抽取：每行一个JSON对象，包含text和entities字段
- 关系抽取：包含text、head_entity、tail_entity、relation字段
- 实体消歧：包含mention、context、candidates、correct_entity字段
- 图嵌入：每行表示一条边，格式为"源节点 目标节点 [权重]"

---

### 4.2.3 任务管理

**功能描述**: 统一管理训练任务和部署任务

**任务类型**:
1. **训练任务**
2. **部署任务**

**训练任务功能**:
- 任务列表展示（任务ID、算法名称、版本、发起人、时间、状态、结果）
- 状态：成功、失败、运行中
- 操作：查看日志、查看详情、重试（失败任务）
- **训练任务详情弹窗**:
  - 基本信息（算法、版本、发起人、状态）
  - 时间信息（开始/结束时间、耗时、评估结果）
  - 训练数据集（名称、大小、记录数）
  - 训练参数配置（完整参数列表）
  - 计算资源配置（GPU类型和数量）
  - 训练结果（成功时显示F1、Precision、Recall、Loss）
  - 失败原因（失败时显示错误详情和建议）

**部署任务功能**:
- 任务列表展示（任务ID、算法名称、版本、服务地址、状态、API入口）
- 状态：运行中、失败
- 操作：查看详情、查看API、重试部署（失败任务）
- **部署任务详情弹窗**:
  - 基本信息（算法、版本、服务名称、命名空间、状态）
  - 时间信息（创建/更新时间、运行时长）
  - 服务访问信息（服务地址、API入口）
  - 部署配置（资源、副本数、并发数、超时时间、自动扩缩容）
  - 服务健康状态（健康副本数、请求数、成功率、平均响应时间）
  - 失败原因（失败时显示错误详情）

**筛选功能**:
- 搜索框：按任务ID或算法名称搜索
- 状态筛选：全部状态、待执行、运行中、成功、失败、已取消

**日志查看**（抽屉式侧边栏）:
- 实时展示训练日志
- 黑底绿字终端风格
- 支持下载日志

---

## 4.3 流程管理模块

### 4.3.1 流程列表

**功能描述**: 管理知识图谱构建流程

**流程分类**:
1. **端到端构建流程** - LLM-Based方案
2. **多阶段构建流程** - 传统Pipeline方案
3. **混合式构建流程** - 传统+LLM混合
4. **领域专用流程** - 针对专业领域优化
5. **增量更新流程** - 增量更新知识图谱

**核心功能**:
- 三级导航：流程分类 → 分类详情 → 流程详情
- 流程信息展示：
  - 名称、类型、版本、状态
  - 适用场景
  - 步骤数、性能指标
  - 更新时间
- 操作：查看详情、部署流程

**流程详情页**:
- 流程基本信息
- 流程步骤列表（每个步骤使用的算法）
- 算法配置参数
- 执行历史
- 性能评估

**流程选择建议**（紫色信息框）:
- LLM-Based流程：标注数据少、快速验证
- 传统流程：大规模数据、成本敏感
- 混合式流程：平衡精度和成本
- 领域流程：专业领域、精度要求高

---

### 4.3.2 部署管理

**功能描述**: 管理流程部署配置

**核心功能**:
- 流程部署配置
- 部署实例管理
- 资源监控
- 部署历史

---

## 4.4 图谱服务模块

### 4.4.1 服务列表

**功能描述**: 管理知识图谱相关服务

**服务类型**:
1. **图谱可视化服务**
2. **在线查询服务**
3. **图谱分析服务**
4. **推荐服务**
5. **问答服务**

**核心功能**:
- 服务卡片展示（名称、类型、状态、版本、API地址）
- 服务详情（配置、监控指标、调用统计）
- 服务操作：启动、停止、重启、查看API
- API文档查看

---

### 4.4.2 API管理

**功能描述**: 统一管理所有API接口

**核心功能**:
- API分类（算法API、查询API、分析API）
- API文档（请求/响应格式、参数说明、示例）
- API密钥管理
- 调用统计和监控
- SDK下载

---

## 4.5 图谱应用模块

### 4.5.1 应用中心

**功能描述**: 提供ChatGPT风格的知识图谱应用

**界面设计**:
- 左侧：应用选择侧边栏
- 右侧：对话区域

**应用列表**:
1. **学者发现** - 发现领域内的重要学者
2. **关系探索** - 探索实体间的关系路径
3. **知识问答** - 基于图谱的智能问答
4. **趋势分析** - 分析领域发展趋势
5. **论文推荐** - 推荐相关学术论文
6. **概念理解** - 解释领域概念

**对话功能**:
- 消息输入框（支持Enter发送，Shift+Enter换行）
- 消息历史展示
- 用户消息：蓝色背景，右对齐
- AI回复：灰色背景，左对齐，带应用图标
- 支持多种结果类型：
  - 文本问答
  - 学者列表（头像、姓名、机构、领域）
  - 关系图谱
  - 实体卡片
  - 趋势图表
  - 论文列表

**交互特性**:
- 自动滚动到最新消息
- 加载状态提示
- 错误提示

---

## 5. 非功能需求

### 5.1 性能要求
- 页面加载时间 < 2秒
- 列表数据刷新 < 1秒
- 训练日志实时更新延迟 < 500ms
- 支持并发用户数 ≥ 100

### 5.2 可用性要求
- 系统可用性 ≥ 99.5%
- 服务响应时间 < 3秒
- 数据备份频率：每日
- 灾难恢复时间 < 4小时

### 5.3 安全要求
- 用户身份认证
- API密钥管理
- 操作日志记录
- 数据加密传输

### 5.4 兼容性要求
- 浏览器支持：Chrome 90+, Firefox 88+, Safari 14+
- 响应式设计，支持1920x1080及以上分辨率
- 支持暗色模式（可选）

---

## 6. 设计规范

### 6.1 视觉设计
- **设计风格**: 专业、简洁、不要花里胡哨的配色
- **主色调**: 
  - 主色：蓝色 (#2563eb)
  - 成功：绿色 (#10b981)
  - 警告：黄色 (#f59e0b)
  - 错误：红色 (#ef4444)
  - 信息：蓝色 (#3b82f6)
- **字体**: 系统默认字体
- **间距**: 遵循8px网格系统

### 6.2 组件规范
- **按钮**:
  - 主要操作：蓝色背景
  - 次要操作：白色背景+边框
  - 危险操作：红色背景
  - 禁用状态：灰色背景+不可点击
- **卡片**: 白色背景，灰色边框，圆角
- **表格**: 斑马纹，悬停高亮
- **标签**: 圆角，不同类型不同颜色
- **信息框**: 
  - 提示（蓝色）
  - 警告（黄色）
  - 成功（绿色）
  - 备注（琥珀色/黄色）

### 6.3 交互规范
- **导航**: 左侧侧边栏固定，深色背景
- **面包屑**: 返回按钮+页面标题
- **Tab切换**: 底部边框指示当前Tab
- **模态框**: 居中显示，半透明遮罩
- **抽屉**: 从右侧滑出，用于日志等详细信息
- **加载状态**: 显示加载指示器
- **空状态**: 友好的提示文案

---

## 7. 技术架构

### 7.1 前端技术栈
- **框架**: React 18 + TypeScript
- **样式**: Tailwind CSS v4
- **图标**: Lucide React
- **图表**: Recharts
- **状态管理**: React Hooks (useState)
- **路由**: 自定义路由系统

### 7.2 组件结构
```
src/
├── app/
│   ├── App.tsx                 # 主应用组件
│   └── components/
│       ├── Sidebar.tsx         # 侧边栏
│       ├── DataDashboard.tsx   # 数据看板
│       ├── AlgorithmCategories.tsx           # 算法分类
│       ├── AlgorithmCategoryDetail.tsx       # 算法分类详情
│       ├── AlgorithmDetailPage.tsx           # 算法详情（含4个Tab）
│       ├── DatasetCategories.tsx             # 数据集分类
│       ├── DatasetCategoryDetail.tsx         # 数据集详情
│       ├── TaskManagement.tsx                # 任务管理
│       ├── PipelineCategories.tsx            # 流程分类
│       ├── PipelineCategoryDetail.tsx        # 流程分类详情
│       ├── PipelineDetail.tsx                # 流程详情
│       ├── PipelineDeployment.tsx            # 流程部署
│       ├── GraphServiceList.tsx              # 图谱服务列表
│       ├── GraphServiceDetail.tsx            # 服务详情
│       ├── ApiManagement.tsx                 # API管理
│       └── ApplicationCenter.tsx             # 应用中心
└── styles/
    ├── theme.css               # 主题样式
    └── fonts.css               # 字体样式
```

### 7.3 数据结构

**算法数据结构**:
```typescript
interface Algorithm {
  id: string;
  name: string;
  englishName: string;
  category: string;
  type: string;
  algorithmType: 'deep-learning' | 'llm' | 'rule-based';
  trainable: boolean;
  version: string;
  status: string;
  owner: string;
  createdAt: string;
  updatedAt: string;
  description: string;
  intro: {
    summary: string;
    scenarios: string[];
    inputFormat: string;
    outputFormat: string;
    performance: {
      f1: string;
      precision: string;
      recall: string;
      speed: string;
    };
    notes: string[];
  };
}
```

**模型数据结构**:
```typescript
interface Model {
  id: string;
  name: string;
  version: string;
  trainDate: string;
  trainTaskId: string;
  dataset: string;
  remark?: string;  // 模型备注
  performance: {
    f1: string;
    precision: string;
    recall: string;
  };
  modelSize: string;
  status: 'deployed' | 'ready' | 'training';
  deployCount: number;
  lastDeployDate?: string;
}
```

**数据集数据结构**:
```typescript
interface Dataset {
  id: string;
  name: string;
  size: string;
  records: number;
  uploadDate: string;
  status: 'ready' | 'processing' | 'error';
  format: string;
  description: string;
  usedBy: string[];
  sampleData?: string;  // Markdown格式的数据示例
}
```

---

## 8. 实现优先级

### P0 (MVP - 已实现)
- ✅ 数据管理模块（数据看板）
- ✅ 算法管理模块（算法列表、算法详情、模型版本、训练、部署）
- ✅ 数据集管理模块（分类管理、上传、数据示例）
- ✅ 任务管理模块（训练任务、部署任务、详情查看）
- ✅ 流程管理模块（流程分类、流程详情）
- ✅ 图谱服务模块（服务列表、API管理）
- ✅ 应用中心模块（ChatGPT风格对话界面）

### P1 (下一阶段)
- 真实后端API集成
- 用户权限管理
- 实时监控Dashboard
- 批量操作功能
- 数据导出功能

### P2 (未来规划)
- 协作功能（评论、分享）
- 通知系统
- 自定义Dashboard
- 移动端适配
- 国际化支持

---

## 9. 关键用户流程

### 9.1 模型训练流程
1. 进入算法管理 → 选择算法分类 → 选择具体算法
2. 点击算法进入详情页
3. 点击"发起训练"按钮
4. 配置训练参数：
   - 选择训练数据集
   - **填写模型备注**（说明此次训练的特点）
   - 配置训练参数（epochs、batch size、learning rate等）
   - 配置计算资源（GPU类型和数量）
5. 点击"开始训练"
6. 在"任务管理"中查看训练进度
7. 训练完成后，在算法详情页的"模型版本"tab查看新生成的模型

### 9.2 模型部署流程
1. 进入算法详情页 → "模型版本"tab
2. 浏览所有模型版本，通过**备注**快速识别需要的模型
3. 点击某个模型的"部署"按钮
4. 配置部署参数（服务名称、资源、副本数等）
5. 点击"开始部署"
6. 在"任务管理"中查看部署状态
7. 部署成功后获得API地址

### 9.3 数据集上传流程
1. 进入数据集管理 → 选择数据集类型
2. 点击"上传数据集"
3. 填写数据集信息（名称、描述）
4. **填写数据示例**（Markdown格式，包含代码块和字段说明）
5. 选择文件上传
6. 上传成功后，数据集出现在列表中
7. 其他用户可以点击"查看示例"了解数据格式

### 9.4 知识问答流程
1. 进入应用中心
2. 选择"知识问答"应用
3. 在输入框中输入问题
4. AI基于知识图谱返回答案
5. 可以继续追问，形成多轮对话

---

## 10. 成功指标

### 10.1 用户指标
- 日活跃用户数 (DAU)
- 月活跃用户数 (MAU)
- 用户留存率
- 功能使用率

### 10.2 业务指标
- 训练任务成功率 ≥ 95%
- 部署任务成功率 ≥ 98%
- 平均模型训练时间
- 平均模型部署时间

### 10.3 质量指标
- 平均模型F1分数 ≥ 85%
- API响应时间 < 200ms
- 系统错误率 < 0.1%

---

## 11. 风险与挑战

### 11.1 技术风险
- 大规模数据处理性能
- GPU资源调度优化
- 分布式训练稳定性
- 模型版本管理复杂度

### 11.2 用户风险
- 学习曲线陡峭
- 数据标注质量参差不齐
- 算法选择困难

### 11.3 应对策略
- 提供详细的文档和示例
- 内置数据质量检查工具
- 提供算法选择建议和最佳实践
- 完善的错误提示和处理

---

## 12. 附录

### 12.1 术语表
- **知识图谱**: 以图结构存储实体和关系的知识库
- **实体抽取**: 从文本中识别命名实体
- **关系抽取**: 识别实体间的语义关系
- **实体消歧**: 将实体链接到标准知识库
- **知识推理**: 基于已有知识推导新知识
- **图嵌入**: 将图节点映射到向量空间

### 12.2 参考资料
- BERT论文: https://arxiv.org/abs/1810.04805
- Node2Vec论文: https://arxiv.org/abs/1607.00653
- 知识图谱综述: https://arxiv.org/abs/2003.02320

---

**文档结束**
